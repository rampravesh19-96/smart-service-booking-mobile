import { PlaceholderScreen } from "@/components/common/PlaceholderScreen";

export function SearchModalScreen() {
  return (
    <PlaceholderScreen
      title="Search Modal"
      subtitle="Modal entry point for quick discovery UX."
      bodyTitle="Modal search placeholder"
      bodyDescription="Use this route for quick search, recent queries, filters, and AI intent suggestions without losing main tab context."
    />
  );
}
